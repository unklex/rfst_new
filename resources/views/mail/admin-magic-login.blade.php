@component('mail::message')
# Вход в админку

Кто-то запросил одноразовую ссылку для входа в админку **{{ config('app.name') }}**.

Если это вы — нажмите на кнопку ниже. Если нет — просто проигнорируйте это письмо, никаких действий не требуется.

@component('mail::button', ['url' => $url])
Войти в админку
@endcomponent

Ссылка действует **{{ $ttlMinutes }} мин.** и сработает только один раз.

---

**Запрос отправлен с IP:** {{ $ipAddress }}

Если вы не запрашивали эту ссылку и видите подозрительную активность — смените пароль администратора.
@endcomponent

/.phpunit.cache
/node_modules
# /public/build is intentionally committed so Beget shared hosting can serve prebuilt
# Vite assets without a Node.js runtime. Rebuild before pushing after any CSS/JS change:
#   npm ci && npm run build && git add -f public/build && git commit
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.claude
